<%
    palette = Undefined.public_send(variant)
    description = variant == 'dark' ? 'UndefinedDark' : 'UndefinedLight'
%>[Background]
Color=<%= palette.bg.to_a.join(',') %>

[BackgroundFaint]
Color=<%= palette.bg.to_a.join(',') %>

[BackgroundIntense]
Color=<%= palette.bg.to_a.join(',') %>

[Color0]
Color=<%= palette.bg.to_a.join(',') %>

[Color0Faint]
Color=<%= palette.bg.to_a.join(',') %>

[Color0Intense]
Color=<%= palette.term8.to_a.join(',') %>

[Color1]
Color=<%= palette.term1.to_a.join(',') %>

[Color1Faint]
Color=<%= palette.term1.to_a.join(',') %>

[Color1Intense]
Color=<%= palette.term9.to_a.join(',') %>

[Color2]
Color=<%= palette.term2.to_a.join(',') %>

[Color2Faint]
Color=<%= palette.term2.to_a.join(',') %>

[Color2Intense]
Color=<%= palette.term10.to_a.join(',') %>

[Color3]
Color=<%= palette.term3.to_a.join(',') %>

[Color3Faint]
Color=<%= palette.term3.to_a.join(',') %>

[Color3Intense]
Color=<%= palette.term11.to_a.join(',') %>

[Color4]
Color=<%= palette.term4.to_a.join(',') %>

[Color4Faint]
Color=<%= palette.term4.to_a.join(',') %>

[Color4Intense]
Color=<%= palette.term12.to_a.join(',') %>

[Color5]
Color=<%= palette.term5.to_a.join(',') %>

[Color5Faint]
Color=<%= palette.term5.to_a.join(',') %>

[Color5Intense]
Color=<%= palette.term13.to_a.join(',') %>

[Color6]
Color=<%= palette.term6.to_a.join(',') %>

[Color6Faint]
Color=<%= palette.term6.to_a.join(',') %>

[Color6Intense]
Color=<%= palette.term14.to_a.join(',') %>

[Color7]
Color=<%= palette.term7.to_a.join(',') %>

[Color7Faint]
Color=<%= palette.term7.to_a.join(',') %>

[Color7Intense]
Color=<%= palette.fg.to_a.join(',') %>

[Foreground]
Color=<%= palette.fg.to_a.join(',') %>

[ForegroundFaint]
Color=<%= palette.fg.to_a.join(',') %>

[ForegroundIntense]
Color=<%= palette.fg.to_a.join(',') %>

[General]
Description=<%= description %>
Opacity=1
Wallpaper=
